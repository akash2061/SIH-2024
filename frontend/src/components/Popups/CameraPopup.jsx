import React from 'react';
import './Popup.css';

const CameraPopup = () => {
    return (
        <div className="popup camera-popup">
            <p>Your camera is being tracked.</p>
        </div>
    );
};

export default CameraPopup;
